function solve(number) {
    var array = [];
    for(var i = 0; i < number; i++){
        array[i] = i * 5;
    }

    for (var o = 0; o < array.length ; o++) {
        console.log(array[o]);
    }
}
